body, html {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    overflow-x: hidden;  /* cegah horizontal scroll */
}

.container {  /* atau class utama kamu */
    max-width: 100%;
    width: 100%;
    padding: 0 16px;  /* padding samping biar tidak nempel pinggir */
    box-sizing: border-box;
}

h1, h2 {  /* judul besar */
    font-size: clamp(2rem, 8vw, 3.5rem);  /* responsive font */
    text-align: center;
}

@media (max-width: 768px) {
    /* tambah style khusus mobile */
    body { font-size: 16px; }
    button { width: 90%; margin: 0 auto; display: block; }
}
